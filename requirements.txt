# XP Robot – Physical AI Hackathon 2026
# Install all dependencies with:
#   pip install -r requirements.txt

# Core simulation
mujoco>=3.1.0

# Computer vision & pose estimation
opencv-python>=4.9.0
mediapipe>=0.10.0

# Object detection (optional but recommended)
ultralytics>=8.0.0      # YOLOv8 – auto-downloads yolov8n.pt on first run

# Numerics (usually already present)
numpy>=1.24.0
